import { Metadata } from "next";

export const metadata: Metadata = {
    title: {
        absolute: "Edit Profile"
    },
}

export default function EditProfile() {
    return(
        <><h1>Edit Profile Page</h1></>
    )
}